// The Auth module primarily relies on the User model for identity.
// This placeholder is kept to align with the module contract and
// can later host auth-specific persistence (e.g. refresh tokens).

module.exports = {};
